# Copy to .env (never commit). Placeholders only.
MYSQL_URL=jdbc:mysql://localhost:3306/kohliiq
MYSQL_USER=kohliiq
MYSQL_PASSWORD=changeme

# Phase 3 Python ingestion (defaults target the project container on :3307)
KOHLIIQ_DB_HOST=127.0.0.1
KOHLIIQ_DB_PORT=3307
KOHLIIQ_DB_USER=root
KOHLIIQ_DB_PASSWORD=changeme
KOHLIIQ_DB_NAME=kohliiq
