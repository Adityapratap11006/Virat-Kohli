# KohliIQ Frontend (React + TypeScript + Tailwind)

- Vite dev server (`npm run dev`), build with `npm run build`.
- Talks only to Spring Boot (`VITE_API_BASE_URL`, default `/api` via dev proxy).
- Dashboard pages/components/services arrive in Phase 6.
